const path = {
  home: "/",
  user: "/user",
  articleDetail: "/article/:slug",
  profile: "/profile",
  editor: "/editor",
  login: "/login",
  register: "/register",
  logout: "/logout",
} as const;

export default path;
